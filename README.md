# incus-mig-script
A script to automate the creation of incus Ubuntu containers using MIG GPUs.

This script requires that you have incus installed and configured as well as the CUDA toolkit. It uses pwgen to generate passwords.

You will need to adjust the Network Default variables to match your network before running this script.
